(defvar     notch_state        "home")
(defvar     workspace_list "[1, 2, 3, 4, 5, 6, 7, 8, 9]")

(deflisten  active_workspace    :initial  1           `scripts/workspace`)
(defvar  window  "kira@nix")
(deflisten  battery             :initial  "[]"        `scripts/battery.sh`)

(defpoll    panel_clock         :interval "5s"        `date "+%H:%M"`)
(defpoll    hour                :interval "5s"        `date "+%H"`)
(defpoll    minutes             :interval "5s"        `date "+%M"`)
